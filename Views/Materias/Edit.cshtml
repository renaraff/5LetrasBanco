@model _5LetrasBanco.Models.Materias


@{
    ViewData["Title"] = "Editar";
}

<h1>Editar as materias</h1>

<hr />
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="MateriasNome" class="control-label"></label>
                <input asp-for="MateriasNome" class="form-control" />
                <span asp-validation-for="MateriasNome" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Salvar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Voltar</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
